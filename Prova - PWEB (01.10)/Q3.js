const temperaturaC = 28;

let temperaturaF = (9/5) * temperaturaC + 32;
    console.log(`${temperaturaF} é a temperatura de Celsius para Fharenheit`);

let temperaturaK = (temperaturaC + 273.15);
    console.log(`${temperaturaK} é a temperatura de Celsius para Kevin`);